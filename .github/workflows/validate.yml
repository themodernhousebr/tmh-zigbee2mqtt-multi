name: Validar repositório

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validar slugs, versões e isolamento
        run: python3 scripts/validate.py

